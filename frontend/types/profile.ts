export type ProfessionalLinkKey = "resume" | "portfolio" | "github" | "linkedin";

export interface ProfessionalLinks {
  resume_url: string | null;
  portfolio_url: string | null;
  github_url: string | null;
  linkedin_url: string | null;
}

